import { Component } from '@angular/core';

@Component({
  selector: 'app-empty',
  standalone: true,
  template: `<div class="p-4 text-muted">Select a category from the menu to view products.</div>`
})
export class EmptyComponent {}
